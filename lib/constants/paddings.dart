import 'package:flutter/material.dart';

class ProjectPaddings {
  static get hSmallPadding => const EdgeInsets.symmetric(horizontal: 5.0);
  static get hMediumPadding => const EdgeInsets.symmetric(horizontal: 20.0);
  static get hLargePadding => const EdgeInsets.symmetric(horizontal: 40.0);

  static get vSmallPadding => const EdgeInsets.symmetric(vertical: 5.0);
  static get vMediumPadding => const EdgeInsets.symmetric(vertical: 20.0);
  static get vLargePadding => const EdgeInsets.symmetric(vertical: 40.0);

  static get gSmallPadding => const EdgeInsets.all(5.0);
  static get gMediumPadding => const EdgeInsets.all(20.0);
  static get gLargePadding => const EdgeInsets.all(40.0);
}

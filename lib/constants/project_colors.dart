import 'package:flutter/material.dart';

abstract class ProjectColors {
  ProjectColors._();
  static const black = Color(0xFF121212);
  static const white = Color(0xFFffffff);
  static const textFamily = 'roboto';
}
